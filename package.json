{
    "name": "frontend2",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "postinstall": "cd frontend; rm -rf package-lock.json; npm install",
        "start": "cd frontend; ls; ls; npm run serve"
    }
}